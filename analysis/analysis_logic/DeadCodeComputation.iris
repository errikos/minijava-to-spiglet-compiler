////////////////////////////////////////////////////////////////////////////////////////////////////
// -- Dead code computation

deadCode(?m, ?i) :-
    varDef(?m, ?i, ?v),
    !liveOut(?m, ?i, ?v).
